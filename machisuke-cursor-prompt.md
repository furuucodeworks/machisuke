# まち助サイト トップページ実装プロンプト（Cursor用）

## プロジェクト概要
「まち助」（Machisuke）は岐阜県美濃加茂市の便利屋サービス。代表：三品貴斗さん。
既存のPeraichiサイトを置き換える、複数ページ構成の新サイトを制作中。
このプロンプトは**トップページ**の実装を依頼するものです。

## 目的
Pencil（デザインツール）で作成したデザイン案を、レスポンシブ対応のHTML + Tailwind CSSに実装してください。
デザインは1つのページに対して、モバイル／iPad／PCの3パターン（ブレークポイント差分）として書き出し済みです。

**最優先事項：忠実な再現。**
- 情報源の優先順位は「①Pencilから書き出したHTML（mobile.html／ipad.html／pc.html）＞②デザインガイドライン＞③本プロンプトのテキスト記載」です。本プロンプト内の「実コンテンツ」セクションは書き出しHTML作成時点の参考メモであり、内容や文言に差異がある場合は必ず書き出しHTML側を正としてください。
- レイアウト、余白、配置順序、文言、配色、フォントは書き出し元・ガイドラインの値をそのまま使用し、独自の解釈やアレンジ、デザインの「改善」「補完」は一切行わないでください。
- 指示にない要素（アイコン、装飾、アニメーション、追加のセクション、追加の文言、独自のマイクロコピーなど）を勝手に追加しないでください。
- 指示にない色・フォント・サイズを推測や好みで使わないでください。必ず本プロンプト記載の変数・値を使用してください。
- 構造やクラス名の整理・リファクタリングは可読性のための最小限にとどめ、見た目・挙動が変わる変更は行わないでください。
- 迷った場合、または情報が不足している場合は、判断で埋めずに一度立ち止まって確認してください。

## 参照ファイル（Pencilからエクスポート済み）
以下3つのHTMLファイルは、同一ページのレスポンシブ差分としてPencilから書き出したものです。
これらを比較し、**1つのHTMLファイルにTailwindのブレークポイント（`sm:` `md:` `lg:`）で統合**してください。

- `~/Desktop/machisuke/machisuke-export/mobile.html`（〜767px想定）
- `~/Desktop/machisuke/machisuke-export/ipad.html`（768〜1023px想定）
- `~/Desktop/machisuke/machisuke-export/pc.html`（1024px〜想定）

※ 書き出しHTMLは1:1の再現なので、クラス名や構造が冗長な場合は意味のある形に整理してください（不要なラッパーの削除、Tailwindのユーティリティクラスへの適切な変換など）。

## 技術構成
- プレーンHTML + Tailwind CSS（フレームワークなし）
- JSは最小限（ハンバーガーメニューの開閉など、必要な場合のみ）
- ビルドツールは不要。Tailwind CDN、またはローカルCLIビルドどちらでも可（プロジェクトの他ページと合わせられる構成にしてください）

## デザイン仕様（Pencil「デザインガイドライン」フレームより正式採用）

### カラー変数（実使用のみ・そのままTailwind configに登録してください）
| 変数名 | HEX | 用途目安 |
|---|---|---|
| `machisuke-orange` | `#E8833A` | アクセント／CTA・バッジ等 |
| `machisuke-green` | `#5FA246` | CTAボタン等 |
| `machisuke-green-dark` | `#4A8A38` | ボタンhover等の濃緑 |
| `machisuke-title-green` | `#3F5A3A` | ロゴ「助」・見出し系の深緑 |
| `machisuke-cream` | `#F7F1E4` | セクション背景 |
| `machisuke-cream-light` | `#FFFAF0` | ヒーロー等の明るいクリーム背景 |
| `machisuke-card-warm-white` | `#FFFAF0` | カード背景 |
| `machisuke-white` | `#FFFFFF` | 基本背景 |
| `machisuke-brown-footer` | `#4A3B2C` | フッター背景 |
| `machisuke-footer-label` | `#EDEDED` | フッター内ラベルテキスト |
| `machisuke-footer-address` | `#C9C2B8` | フッター内住所等サブテキスト |
| `machisuke-text` | `#3A3A3A` | 基本テキスト |
| `machisuke-text-gray` | `#7A7A7A` | サブテキスト・キャプション |
| `machisuke-blue` | `#3E7FB0` | 補助アクセント（必要箇所のみ） |
| `machisuke-border-light` | `#E0E0E0` | ボーダー・区切り線 |

- ロゴ「まち」＝赤 `#A8302A`、「助」＝ `machisuke-title-green` (`#3F5A3A`)（ロゴ色はガイドラインのカラー変数外の固有色として扱う）

### タイポグラフィ変数
| 変数名 | フォント | 用途 |
|---|---|---|
| `$font-display` | Zen Maru Gothic | 見出し・キャッチコピーなど display 用途 |
| `$font-body` | Zen Kaku Gothic New | 本文・通常テキスト |
| `$font-label` | Kosugi Maru | ボタンラベル・バッジ・カードのラベル等 |

3フォントとも `tailwind.config.js` の `theme.extend.fontFamily` に上記変数名で登録し、Google Fontsから読み込んでください。

### その他デザイン方針
- ヒーローセクションはクリーム背景（`machisuke-cream-light`）、ロゴのみ配置（オレンジ塗りなし）
- CTAボタンは緑（`machisuke-green`、hover時 `machisuke-green-dark`）
- 全体トーンは温かみのある手描き風、地域密着型サービスらしい親しみやすさ
- **画像・イラスト素材**：クライアント提供のスティックフィギュアイラスト・ロゴをそのまま使用（AI生成イラストは使わない）
  - 太めの手描き輪郭、水彩風の柔らかいテクスチャ
- **ヘッダー**：横型ロゴを使用

## ページ構成（トップページ内のセクション、Pencil上のフレーム名と対応）
1. `Header` — ヘッダー（ロゴ、ナビ）
2. `Hero` — キャッチコピー「暮らしの『困った』を、まち助が解決！」
3. `IntroSection` — 導入文
4. `TroubleSection` — 「こんなお困りごとはありませんか？」（屋内サポート／屋外サポート／力仕事・作業系／不用品回収／その他／オプションのカテゴリ表、料金例あり）
5. `ReasonSection` — 「まち助が選ばれる理由」5カード（地域密着／親切・丁寧／明朗料金／安心・信頼／小さなご依頼大歓迎、Lucideアイコン使用）
6. `AreaSection` — 対応エリア（地図）
7. `CtaWrap` — お問い合わせCTA
8. `Footer` — フッター（会社情報、代表者名、電話番号など）

## 実装時の注意点
- **繰り返しになりますが、独自判断での装飾・追加・省略は禁止です。** 書き出しHTMLの内容を最優先で忠実に再現してください（デザインガイドライン、本プロンプトの記載はそれを補うものです）。
- テキスト・写真・料金などのコンテンツは、書き出しHTML（mobile.html／ipad.html／pc.html）に含まれる内容をそのまま使用してください。「実コンテンツ」セクションは参考メモであり、内容が食い違う場合は書き出しHTML側が正です。
- 画像・イラスト等の必要なアセットは `~/Desktop/machisuke/public` フォルダに保存済みです。実装時は新規生成・仮画像の使用はせず、`~/Desktop/machisuke/public` フォルダ内の既存ファイルを参照してください。ファイル名・パスが不明な場合はフォルダ内を確認してから当てはめてください。
- SEO・検索性の向上が今回のサイトリニューアルの目的の一つです。適切な見出しタグ（h1/h2/h3）、altテキスト、セマンティックHTML（header/main/section/footer等）を意識してください。

## 実コンテンツ（参考メモ／優先度は書き出しHTMLより下）
※以下は既存Peraichiサイトの文言を参考としてまとめたものです。**書き出しHTML（mobile.html／ipad.html／pc.html）に記載の文言と相違がある場合は、書き出しHTML側を採用してください。** 本セクションはあくまで補助的な参照情報です。

### ページタイトル／メタ情報
- ページ名：`便利屋まち助｜美濃加茂市・可児市の便利屋・草刈り・不用品回収`

### Hero / Intro
- キャッチコピー：「暮らしの『困った』を、まち助が解決！」

### IntroSection
> 大きな工事や専門業者を呼ぶほどではないけれど、一人では大変な作業や時間がなくて困っていることはありませんか？
>
> まち助は、お客様一人ひとりのお困りごとに寄り添い、丁寧で誠実な対応を心がけています。
>
> 「こんなこと頼んでいいのかな？」
>
> そんな内容でもまずはお気軽にご相談ください。
>
> 地域の皆さまの暮らしを支える身近な存在として、全力でお手伝いさせていただきます。

### TroubleSection「こんなお困りごとはありませんか？」
**カテゴリ表（屋内サポート／屋外サポート／力仕事・作業系／不用品回収／その他／オプション）は1枚の画像データとして`public`フォルダに用意されています。HTML/CSSでテーブルやグリッドを組んで再現するのではなく、その画像をそのまま配置してください。** 以下は画像内に含まれる内容の参考メモです（画像ファイル特定や代替テキスト(alt)作成の参考用）。

補足コピー：
> 小さなお困りごとからお気軽に👍
>
> 「電球を交換してほしい」
> 「草むしりを手伝ってほしい」
> 「家具を移動したい」
> 「高齢の親の様子を見てきてほしい」
>
> そんな日常の「ちょっと困った」を気軽に頼める地域密着の便利屋です。

### ReasonSection「まち助が選ばれる理由」
**5カードのアイコンも画像データとして`public`フォルダに用意されています。SVGアイコンライブラリ（Lucide等）で作成・代替するのではなく、その画像をそのまま配置してください。**
5カード（画像アイコン使用）：
1. **地域密着** — 地元業者だからすぐに駆けつけます！
2. **親切・丁寧** — 一つひとつご依頼を大切に対応します。
3. **明朗料金** — 事前にお見積り・お会計は安心の後払い定額
4. **安心・信頼** — 誠実な対応で安心してお任せいただけます。
5. **小さなご依頼大歓迎** — 「これくらい頼んでいいのかな？」そんなご相談もお気軽に。

### Footer / 会社情報
- 屋号：便利屋まち助（ロゴ：「まち」赤／「助」緑）
- 所在地：岐阜県美濃加茂市
- 代表：三品貴斗
- 電話番号：090-2343-9557
- 対応エリア：美濃加茂市・可児市（周辺エリアも含む想定、コピーに準拠）

### ナビゲーション／その他要素
- 「お問い合わせ」CTAボタン（オレンジ）
- インスタグラムへの導線あり（Instagramページとして別途実装予定、トップページ上ではアイコンリンク程度）
- 「View more」ボタン（各セクションの詳細展開用、既存サイトのUIパターン）
- 将来的に他ページ（Service/Pricing、About、Instagram、FAQ、Contact）も同じデザインシステムで実装するため、色・フォントサイズ・余白などは再利用しやすいようTailwindの設定（`tailwind.config.js`のtheme拡張）に定義してください。
- レスポンシブの境界は、3つの書き出しHTMLの実際のレイアウト差分を見て判断してください（上記のpx値は目安です）。

## 成果物
- `index.html`（トップページ、レスポンシブ対応済み）
- `tailwind.config.js`（上記カラー変数・フォント変数をtheme.extendに登録したテーマ設定）
- 画像・アセットは新規作成せず、`~/Desktop/machisuke/public` フォルダ内に保存済みの既存ファイルを参照して使用

## 進め方
1. 3つのエクスポートHTMLを読み込み、構造とスタイルの差分を洗い出す
2. 共通のセマンティックHTML構造を設計する
3. Tailwindのブレークポイントで差分を吸収しながら1ファイルに統合する
4. ブラウザで3サイズ（375px / 768px / 1440px程度）を確認し、崩れがないかチェックする
5. 最終確認：書き出しHTML・デザインガイドライン・本プロンプトと見比べ、追加/省略/改変した箇所がないか照合する
